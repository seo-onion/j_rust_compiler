fn fib(n: i64) -> i64 {
    if n < 2 {
        return n;
    } else {
        return fib(n - 1) + fib(n - 2);
    }
}

fn fact(n: i64) -> i64{
    if n<2 {
        return 1;
    }
    return n * fact(n-1);
}

fn main() {
    let mut x: i64;
    x=1;
    print("{}",fact(15));
}
